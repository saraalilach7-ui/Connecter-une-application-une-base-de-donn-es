Dans ce chapitre nous avons appris à utiliser les requêtes préparées avec PDO pour sécuriser les opérations sur la base de données les méthodes prepare() et execute() permettent de séparer la requête SQL des données on peut utiliser des paramètres nommés comme :nom ou anonymes comme ? les méthodes bindParam() et bindValue() servent à lier les valeurs aux paramètres les requêtes préparées protègent contre les injections SQL et améliorent les performances lors de plusieurs exécutions