Dans ce chapitre nous avons appris à insérer modifier et supprimer des enregistrements dans une table avec PDO on utilise INSERT INTO pour ajouter UPDATE pour modifier et DELETE pour supprimer en faisant attention à WHERE pour ne pas toucher toutes les lignes on exécute les requêtes avec prepare et execute on peut vérifier le nombre de lignes affectées avec rowCount et try catch permet de gérer les erreurs et afficher les messages PDO de façon sécurisée